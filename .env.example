# PostHog analytics (US cloud). Project API key from
# https://us.posthog.com -> Settings -> Project -> Project API Key.
# Set in Vercel project env vars for production; copy to .env.local for dev.
# Leave unset to disable analytics entirely (all tracking no-ops).
NEXT_PUBLIC_POSTHOG_KEY=
